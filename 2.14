import math  

def area_of_circle(radius):
    return math.pi * radius ** 2  # Area formula: πr²


r = float(input("Enter the radius of the circle: "))
print(f"Area of the circle: {area_of_circle(r):.2f}")
